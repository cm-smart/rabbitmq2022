server:
  port: 8888

spring:
  application:
    name: springboot-rabbitmq-provider

  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
    virtual-host: /
    #确认消息已发送到交换机
    publisher-confirm-type: correlated
    #确认消息已发送到队列
    publisher-returns: true